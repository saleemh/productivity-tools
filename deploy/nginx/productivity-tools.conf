# Drop this inside your existing server { ... } block for saleem.net

location ^~ /productivity-tools/ {
    alias /var/www/productivity-tools/;
    index index.html;
    try_files $uri $uri/ =404;
    autoindex off;
}

# Optional staging path for initial verification
# location ^~ /productivity-tools-test/ {
#     alias /var/www/productivity-tools-test/;
#     index index.html;
#     try_files $uri $uri/ =404;
#     autoindex off;
# }
